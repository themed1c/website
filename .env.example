# Cloudflare Pages > Settings > Variables and Secrets.
# Add BOTH as Secret (encrypted), not plaintext, for Production and Preview.
MELODY_SCRIPT_URL=https://script.google.com/macros/s/REPLACE_ME/exec
MELODY_SHARED_SECRET=REPLACE_ME
